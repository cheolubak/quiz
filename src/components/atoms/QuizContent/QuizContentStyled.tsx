import styled from '@emotion/styled';

export const QuizContentStyled = styled.pre`
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.4em;
  color: #000;
  white-space: pre-wrap;
  word-break: break-all;
  margin: 0 0 16px;
`;
